/*
 * WARNING
 *
 * This file is maintained automatically by Bndtools.
 * You should not edit it.
 *
 * See the BUILDING-GRADLE.md document in the root project for more details.
 *
 * Setup the javadoc options.
 *
 * Note: the 'java' plugin must have been applied to the project before this
 *       file can be applied.
 */

assert(project != rootProject                           )
assert(hasProperty('gradleBuildJavadocAuthor'          ))
assert(hasProperty('gradleBuildJavadocBreakIterator'   ))
assert(hasProperty('gradleBuildJavadocClassPathBoot'   ))
assert(hasProperty('gradleBuildJavadocDir'             ))
assert(hasProperty('gradleBuildJavadocDocFilesSubDirs' ))
assert(hasProperty('gradleBuildJavadocEncoding'        ))
assert(hasProperty('gradleBuildJavadocFailOnError'     ))
assert(hasProperty('gradleBuildJavadocMaxMemory'       ))
assert(hasProperty('gradleBuildJavadocMemberLevel'     ))
assert(hasProperty('gradleBuildJavadocNoComment'       ))
assert(hasProperty('gradleBuildJavadocNoDeprecated'    ))
assert(hasProperty('gradleBuildJavadocNoDeprecatedList'))
assert(hasProperty('gradleBuildJavadocNoHelp'          ))
assert(hasProperty('gradleBuildJavadocNoIndex'         ))
assert(hasProperty('gradleBuildJavadocNoNavBar'        ))
assert(hasProperty('gradleBuildJavadocNoSince'         ))
assert(hasProperty('gradleBuildJavadocNoTimestamp'     ))
assert(hasProperty('gradleBuildJavadocNoTree'          ))
assert(hasProperty('gradleBuildJavadocSplitIndex'      ))
assert(hasProperty('gradleBuildJavadocUse'             ))
assert(hasProperty('gradleBuildJavadocVerbose'         ))
assert(hasProperty('gradleBuildJavadocVersion'         ))
assert(hasProperty('sourceSets'                        ))


javadoc.destinationDir                     = new File(buildDir, "${gradleBuildJavadocDir}")
javadoc.source                             = sourceSets.main.allJava
javadoc.title                              = "${project.name}"
javadoc.failOnError                        = gradleBuildJavadocFailOnError
javadoc.maxMemory                          = gradleBuildJavadocMaxMemory
javadoc.verbose                            = gradleBuildJavadocVerbose

configure(javadoc.options) {
  docTitle                                 = "${project.name}"
  windowTitle                              = "${project.name}"

  author                                   = gradleBuildJavadocAuthor
  bootClasspath                            = gradleBuildJavadocClassPathBoot
  breakIterator                            = gradleBuildJavadocBreakIterator
  docFilesSubDirs                          = gradleBuildJavadocDocFilesSubDirs
  encoding                                 = gradleBuildJavadocEncoding
  memberLevel                              = gradleBuildJavadocMemberLevel
  noComment                                = gradleBuildJavadocNoComment
  noDeprecated                             = gradleBuildJavadocNoDeprecated
  noDeprecatedList                         = gradleBuildJavadocNoDeprecatedList
  noHelp                                   = gradleBuildJavadocNoHelp
  noIndex                                  = gradleBuildJavadocNoIndex
  noNavBar                                 = gradleBuildJavadocNoNavBar
  noSince                                  = gradleBuildJavadocNoSince
  noTimestamp                              = gradleBuildJavadocNoTimestamp
  noTree                                   = gradleBuildJavadocNoTree
  splitIndex                               = gradleBuildJavadocSplitIndex
  use                                      = gradleBuildJavadocUse
  version                                  = gradleBuildJavadocVersion
}
